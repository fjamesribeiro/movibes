name: 🚀 Deploy Movibes para VPS

on:
  push:
    branches: [ main ]
  workflow_dispatch:  # Adiciona botão manual no GitHub

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: 🚀 Deploy via SSH
      uses: appleboy/ssh-action@v1.0.0
      with:
        host: ${{ secrets.VPS_HOST }}
        username: ${{ secrets.VPS_USER }}
        key: ${{ secrets.VPS_SSH_KEY }}
        port: ${{ secrets.VPS_PORT }}
        timeout: 300s # <-- Mantenha o timeout que corrigimos!

        script: |
          cd /var/www/api/movibes
          bash deploy.sh

    - name: ✅ Notificar sucesso
      if: success()
      uses: appleboy/telegram-action@v0.1.1
      with:
        to: ${{ secrets.TELEGRAM_CHAT_ID }}
        token: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        format: markdown
        message: |
          🎉 *Deploy Concluído com Sucesso!*

          🚀 **Projeto:** Movibes
          👤 **Autor:** James Ribeiro

          *Commit:*
          ${{ github.event.head_commit.message }}

          🌐 *Aplicação disponível em:* https://movibes.linexa.com.br

    - name: ❌ Notificar falha
      if: failure()
      uses: appleboy/telegram-action@v0.1.1
      with:
        to: ${{ secrets.TELEGRAM_CHAT_ID }}
        token: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        format: markdown
        message: |
          ❌ *ERRO NO DEPLOY!*

          O deploy do projeto *Movibes* falhou.

          Verifique os logs na página da Action:
          ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
